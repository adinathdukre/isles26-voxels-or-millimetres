"""Namespace for the ISLES'26 organisers' evaluation code.

`eval_utils.py` is NOT redistributed here -- see official/README.md for where to
get it. The scorers in the parent directory add `analysis/official` to sys.path
and do `from eval_utils import ...`, so dropping the organisers' file in beside
this one is all that is needed.
"""
